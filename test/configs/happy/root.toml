


imports = [
    { import = "includes/include1.toml" },
    { import = "includes/include2.toml", as = "custom_name_include2" },
    { from = "includes/include3.toml", import = [ "section_b"] },
]

[section_a]
    imports = [
        { import = "includes/include1.toml" },
        { import = "includes/include2.toml", as = "custom_name_include2" },
        { from = "includes/include3.toml", import = ["section_a", "section_b"] },
        { from = "includes/include3.toml", import = "section_a", as = "renamed_section_a" },
    ]

    section_a_val = "root_section_a_value"
